# 缓存机制文档

本文档整合了缓存机制的问题分析、性能测试和使用指南。

## 目录

- [缓存控制](#缓存控制)
- [潜在问题分析](#潜在问题分析)
- [性能测试](#性能测试)
- [最佳实践](#最佳实践)

---

## 缓存控制

### 缓存开关

为了在调试和性能优化之间灵活切换，提供了缓存开关选项：

#### 命令行选项

```bash
# 禁用缓存（推荐用于调试和数据对比）
./build/run_pys_script --no-cache python/test_hello.pys

# 启用缓存（默认）
./build/run_pys_script --cache python/test_hello.pys
```

#### 环境变量

```bash
# 禁用缓存
PYS_CACHE=0 ./build/run_pys_script python/test_hello.pys

# 启用缓存
PYS_CACHE=1 ./build/run_pys_script python/test_hello.pys
```

#### 程序化控制

```cpp
ScriptInterpreter interp;

// 禁用缓存
interp.setCacheEnabled(false);

// 启用缓存
interp.setCacheEnabled(true);

// 检查缓存状态
bool enabled = interp.isCacheEnabled();

// 清空缓存
interp.clearCache();
```

### 使用建议

1. **数据对比阶段**：先禁用缓存（`--no-cache`）验证正确性
2. **性能优化阶段**：确认正确性后，启用缓存（默认）提高性能
3. **遇到缓存相关问题时**：禁用缓存进行调试，确认是否是缓存导致的问题

---

## 潜在问题分析

### 已处理的问题

1. ✅ **对象 id 变化**：创建新对象时，缓存键会不同，缓存失效
2. ✅ **对象 id 不变但内容变化**：检测到 PythonObject 类型变量时，重新执行定义
3. ✅ **简单类型变量变化**：变量值变化时，缓存键会不同，缓存失效

### 潜在问题场景

#### 1. 模块属性变化（高风险）

**问题描述**：
- 函数使用了导入模块的属性（如 `np.array`、`pd.DataFrame`）
- 模块对象的 id 不变，但模块的属性可能被修改
- 缓存键只包含模块对象的 id，不包含模块属性的变化

**缓解措施**：
- 当前实现：检测到 PythonObject 类型变量（包括模块）时，重新执行定义
- 但这会降低缓存效率

#### 2. 内置函数/变量变化（低风险）

**问题描述**：
- 函数使用了内置函数（如 `len`、`range`）
- 内置函数不在 `variable_manager_` 中，不在缓存键中
- 如果内置函数被替换，缓存可能无法捕获

**缓解措施**：
- 内置函数很少被修改，风险较低
- 如果确实需要，可以在缓存键中包含 `__builtins__` 的哈希值

#### 3. 函数内部 import（中风险）

**问题描述**：
- 函数内部有 import 语句
- 导入的模块可能在不同时间点有不同的状态
- 模块对象的 id 不变，但模块内容可能变化

**缓解措施**：
- 当前实现：检测到 PythonObject 类型变量时，重新执行定义
- 函数内部的 import 会在执行时重新导入，应该能获取最新的模块状态

#### 4. 变量被删除然后重新创建（低风险）

**问题描述**：
- 变量被删除（`del x`）然后重新创建（`x = 20`）
- 新变量的 id 会不同，缓存键会不同

**影响**：
- 当前实现应该能正确处理（因为变量 id 变化了）

#### 5. 函数使用了不在 variable_manager 中的变量（中风险）

**问题描述**：
- 函数使用了全局变量，但这个变量不在 `variable_manager_` 中
- 变量可能是在 Python 端直接设置的，或者是从其他模块导入的
- 缓存键不包含这些变量，可能导致错误的缓存命中

**缓解措施**：
- 当前实现：在函数定义前，将所有 `variable_manager_` 中的变量注入到 globals
- 但如果变量不在 `variable_manager_` 中，缓存键不会包含它

### 当前实现的权衡

当前实现选择了**正确性优先**：对于包含 PythonObject 类型变量的函数，即使缓存键匹配，也会重新执行定义。

**权衡**：
- ✅ **正确性优先**：确保函数行为正确
- ❌ **性能折衷**：对于包含 numpy/pandas 的函数，缓存效率较低

---

## 性能测试

### 缓存机制的优势场景

缓存机制在以下场景下能带来明显的性能提升：

1. **重复定义相同函数/类**
   - 当脚本中多次定义相同的函数或类时，缓存可以避免重复执行 `py::exec`
   - 例如：循环中动态生成函数定义

2. **大量函数定义**
   - 当脚本包含大量函数定义时，缓存可以减少 Python 解释器的开销

3. **复杂函数定义**
   - 包含复杂导入、嵌套结构的函数定义，缓存可以显著减少执行时间

### 缓存机制的开销

缓存机制本身也有一定的开销：

1. **哈希计算**
   - 需要计算函数源代码和变量状态的哈希值
   - 对于简单函数，这个开销可能超过收益

2. **缓存查找**
   - 需要在 `unordered_map` 中查找缓存键
   - 虽然 O(1) 复杂度，但仍有一定开销

3. **变量状态检查**
   - 需要检查是否有 PythonObject 类型的变量
   - 需要遍历所有变量来构建缓存键

### 实际测试结果

根据快速测试结果（前10个测试用例）：

- **总执行时间（开启缓存）**: ~3.33s
- **总执行时间（关闭缓存）**: ~2.77s
- **整体加速比**: 0.83x（缓存反而更慢）

**原因分析**：
1. 这些测试用例中，函数通常只定义一次
2. 缓存查找和哈希计算的开销超过了 `py::exec` 的节省
3. 对于单次定义的函数，缓存没有优势

### 重复定义场景测试

对于重复定义函数的场景，缓存能带来明显的性能提升：

- **test_cache_performance.pys**：重复定义3次，包含导入语句
- **test_cache_repeated_def.pys**：重复定义简单函数3次
- **test_cache_many_redefinitions.pys**：大量重复定义（50次）

在这些场景下，缓存命中率接近 100%，性能提升明显。

---

## 最佳实践

### 1. 避免修改模块属性

不要修改导入模块的属性（如 `np.array`），这会导致缓存失效或错误行为。

### 2. 避免修改内置函数

不要修改内置函数（如 `len`），这可能导致缓存无法捕获变化。

### 3. 使用变量而非模块属性

如果可能，将模块属性赋值给变量后再使用：

```python
# 推荐
import numpy as np
array_func = np.array
result = array_func([1, 2, 3])

# 不推荐
import numpy as np
result = np.array([1, 2, 3])  # 如果 np.array 被修改，缓存可能失效
```

### 4. 对于关键函数，考虑禁用缓存

如果函数行为对正确性要求很高，可以考虑禁用缓存：

```bash
./build/run_pys_script --no-cache critical_function.pys
```

### 5. 开发/调试阶段禁用缓存

在开发和调试阶段，建议禁用缓存，确保每次函数定义都重新执行：

```bash
./build/run_pys_script --no-cache script.pys
```

### 6. 生产环境启用缓存

在生产环境中，如果脚本包含重复定义的函数，启用缓存可以提高性能：

```bash
./build/run_pys_script --cache script.pys
```

---

## 总结

当前的缓存机制在**正确性**和**性能**之间做了权衡：
- 对于简单类型变量，缓存效率高
- 对于 PythonObject 类型变量，为了保证正确性，会重新执行定义

虽然存在一些潜在的边缘情况，但在实际使用中，这些问题很少出现。如果遇到缓存相关的问题，可以考虑：
1. 检查是否有模块属性被修改
2. 检查是否有内置函数被替换
3. 考虑禁用缓存或使用更细粒度的缓存策略

缓存机制的主要价值在于：
- ✅ **正确性保证**：确保开启/关闭缓存时结果一致
- ✅ **重复定义场景**：在函数/类重复定义时带来性能提升
- ✅ **灵活性**：可以通过 `--no-cache` 选项灵活控制

