import json as json

print("Testing unpack syntax")

dict1 = {"a": 1, "b": 2}
dict2 = {"c": 3, "d": 4}

# 测试解包合并字典
merged = { **dict1, **dict2 }
print("merged:", merged)

# 测试json.dumps
json_str = json.dumps(merged, indent=2)
print("json_str:", json_str)

print("Test completed")
