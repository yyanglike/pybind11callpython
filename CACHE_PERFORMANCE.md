# 缓存性能测试说明

## 脚本说明

项目根目录下提供了以下脚本用于缓存性能测试：

1. **`verify_cache_consistency.sh`** - 完整验证脚本
   - 验证所有测试用例在开启/关闭缓存时结果是否一致
   - 统计性能差异
   - 运行时间：约5-10分钟（107个测试用例）

2. **`verify_cache_quick.sh`** - 快速性能对比脚本
   - 仅测试前10个测试用例
   - 快速了解缓存性能效果
   - 运行时间：约10-30秒

3. **`test_cache_performance.pys`** - 专门用于测试缓存性能的脚本
   - 包含多次重复定义的函数
   - 用于验证缓存在重复定义场景下的性能提升

## 使用方法

### 完整验证（推荐）

```bash
./verify_cache_consistency.sh
```

### 快速测试

```bash
./verify_cache_quick.sh
```

### 测试特定脚本

```bash
# 开启缓存
time ./build/run_pys_script test_cache_performance.pys

# 关闭缓存
time ./build/run_pys_script --no-cache test_cache_performance.pys
```

## 性能分析

### 缓存机制的优势场景

缓存机制在以下场景下能带来明显的性能提升：

1. **重复定义相同函数/类**
   - 当脚本中多次定义相同的函数或类时，缓存可以避免重复执行 `py::exec`
   - 例如：循环中动态生成函数定义

2. **大量函数定义**
   - 当脚本包含大量函数定义时，缓存可以减少 Python 解释器的开销

3. **复杂函数定义**
   - 包含复杂导入、嵌套结构的函数定义，缓存可以显著减少执行时间

### 缓存机制的开销

缓存机制本身也有一定的开销：

1. **哈希计算**
   - 需要计算函数源代码和变量状态的哈希值
   - 对于简单函数，这个开销可能超过收益

2. **缓存查找**
   - 需要在 `unordered_map` 中查找缓存键
   - 虽然 O(1) 复杂度，但仍有一定开销

3. **变量状态检查**
   - 需要检查是否有 PythonObject 类型的变量
   - 需要遍历所有变量来构建缓存键

### 实际测试结果

根据快速测试结果（前10个测试用例）：

- **总执行时间（开启缓存）**: ~3.33s
- **总执行时间（关闭缓存）**: ~2.77s
- **整体加速比**: 0.83x（缓存反而更慢）

**原因分析**：
1. 这些测试用例中，函数通常只定义一次
2. 缓存查找和哈希计算的开销超过了 `py::exec` 的节省
3. 对于单次定义的函数，缓存没有优势

### 建议

1. **开发/调试阶段**：使用 `--no-cache` 禁用缓存
   - 确保每次函数定义都重新执行
   - 避免缓存导致的问题

2. **生产环境**：根据实际情况决定
   - 如果脚本中有大量重复定义的函数，启用缓存
   - 如果函数只定义一次，可以考虑禁用缓存

3. **性能关键场景**：进行实际测试
   - 使用 `verify_cache_consistency.sh` 或 `verify_cache_quick.sh` 进行测试
   - 根据实际场景的性能数据决定是否启用缓存

## 性能优化建议

如果发现缓存没有带来性能提升，可以考虑：

1. **优化缓存键计算**
   - 减少变量状态的哈希计算开销
   - 只包含影响函数行为的变量

2. **延迟缓存**
   - 只在函数被多次定义时才启用缓存
   - 避免单次定义的开销

3. **批量缓存**
   - 对于大量函数定义，可以考虑批量处理

## 总结

缓存机制的主要价值在于：
- ✅ **正确性保证**：确保开启/关闭缓存时结果一致
- ✅ **重复定义场景**：在函数/类重复定义时带来性能提升
- ✅ **灵活性**：可以通过 `--no-cache` 选项灵活控制

对于单次定义的函数，缓存可能不会带来性能提升，甚至可能略慢，但这是可以接受的权衡，因为：
- 缓存机制保证了正确性
- 在合适的场景下（重复定义）能带来性能提升
- 可以通过开关灵活控制

