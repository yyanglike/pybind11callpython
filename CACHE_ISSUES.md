# 缓存机制潜在问题分析

## 概述

当前的函数/类定义缓存机制基于函数源代码和 `variable_manager_` 中所有变量的哈希值。虽然已经处理了一些边界情况，但仍存在一些可能导致缓存问题的场景。

## 缓存控制

### 缓存开关

为了在调试和性能优化之间灵活切换，提供了缓存开关选项：

#### 命令行选项

```bash
# 禁用缓存（推荐用于调试和数据对比）
./build/run_pys_script --no-cache python/test_hello.pys

# 启用缓存（默认）
./build/run_pys_script --cache python/test_hello.pys
```

#### 环境变量

```bash
# 禁用缓存
PYS_CACHE=0 ./build/run_pys_script python/test_hello.pys

# 启用缓存
PYS_CACHE=1 ./build/run_pys_script python/test_hello.pys
```

#### 程序化控制

```cpp
ScriptInterpreter interp;

// 禁用缓存
interp.setCacheEnabled(false);

// 启用缓存
interp.setCacheEnabled(true);

// 检查缓存状态
bool enabled = interp.isCacheEnabled();

// 清空缓存
interp.clearCache();
```

### 使用建议

1. **数据对比阶段**：先禁用缓存（`--no-cache`）验证正确性
2. **性能优化阶段**：确认正确性后，启用缓存（默认）提高性能
3. **遇到缓存相关问题时**：禁用缓存进行调试，确认是否是缓存导致的问题

## 已处理的问题

1. ✅ **对象 id 变化**：创建新对象时，缓存键会不同，缓存失效
2. ✅ **对象 id 不变但内容变化**：检测到 PythonObject 类型变量时，重新执行定义
3. ✅ **简单类型变量变化**：变量值变化时，缓存键会不同，缓存失效

## 潜在问题场景

### 1. 模块属性变化（高风险）

**问题描述**：
- 函数使用了导入模块的属性（如 `np.array`、`pd.DataFrame`）
- 模块对象的 id 不变，但模块的属性可能被修改
- 缓存键只包含模块对象的 id，不包含模块属性的变化

**示例**：
```python
import numpy as np

def f():
    return np.array([1, 2, 3]).sum()

# 如果 np.array 被替换了
np.array = lambda x: [0]  # 模块属性变化，但模块对象 id 不变

def f():  # 缓存可能命中，但函数行为会改变
    return np.array([1, 2, 3]).sum()
```

**影响**：
- 缓存可能错误地命中
- 函数行为可能不正确

**缓解措施**：
- 当前实现：检测到 PythonObject 类型变量（包括模块）时，重新执行定义
- 但这会降低缓存效率

### 2. 内置函数/变量变化（低风险）

**问题描述**：
- 函数使用了内置函数（如 `len`、`range`）
- 内置函数不在 `variable_manager_` 中，不在缓存键中
- 如果内置函数被替换，缓存可能无法捕获

**示例**：
```python
def f():
    return len([1, 2, 3])

# 如果 len 被替换了
len = lambda x: 999

def f():  # 缓存可能命中，但函数行为会改变
    return len([1, 2, 3])
```

**影响**：
- 缓存可能错误地命中
- 函数行为可能不正确

**缓解措施**：
- 内置函数很少被修改，风险较低
- 如果确实需要，可以在缓存键中包含 `__builtins__` 的哈希值

### 3. 函数内部 import（中风险）

**问题描述**：
- 函数内部有 import 语句
- 导入的模块可能在不同时间点有不同的状态
- 模块对象的 id 不变，但模块内容可能变化

**示例**：
```python
def f():
    import math
    return math.pi

# 如果 math 模块被修改了
import math
math.pi = 3.14  # 修改模块属性

def f():  # 缓存可能命中，但函数行为会改变
    import math
    return math.pi
```

**影响**：
- 缓存可能错误地命中
- 函数行为可能不正确

**缓解措施**：
- 当前实现：检测到 PythonObject 类型变量时，重新执行定义
- 函数内部的 import 会在执行时重新导入，应该能获取最新的模块状态

### 4. 变量被删除然后重新创建（低风险）

**问题描述**：
- 变量被删除（`del x`）然后重新创建（`x = 20`）
- 新变量的 id 会不同，缓存键会不同
- 但如果其他变量没变，缓存键可能相同

**示例**：
```python
x = 10

def f():
    return x

del x
x = 20  # 重新创建，新的 id

def f():  # 缓存键会不同（因为 x 的 id 变化了）
    return x
```

**影响**：
- 当前实现应该能正确处理（因为变量 id 变化了）

### 5. 函数使用了不在 variable_manager 中的变量（中风险）

**问题描述**：
- 函数使用了全局变量，但这个变量不在 `variable_manager_` 中
- 变量可能是在 Python 端直接设置的，或者是从其他模块导入的
- 缓存键不包含这些变量，可能导致错误的缓存命中

**示例**：
```python
# 在 Python 端直接设置变量（不在 variable_manager_ 中）
import sys
sys.modules['__main__'].__dict__['x'] = 10

def f():
    return x

sys.modules['__main__'].__dict__['x'] = 20

def f():  # 缓存可能命中，但函数行为会改变
    return x
```

**影响**：
- 缓存可能错误地命中
- 函数行为可能不正确

**缓解措施**：
- 当前实现：在函数定义前，将所有 `variable_manager_` 中的变量注入到 globals
- 但如果变量不在 `variable_manager_` 中，缓存键不会包含它

### 6. 模块对象的 id 不变但模块内容变化（高风险）

**问题描述**：
- 模块对象的 id 不变，但模块的属性被修改
- 缓存键只包含模块对象的 id，不包含模块属性的变化

**示例**：
```python
import numpy as np
np_id = id(np)

def f():
    return np.array([1, 2, 3]).sum()

# 修改模块属性
np.array = lambda x: [0]

def f():  # 缓存可能命中，但函数行为会改变
    return np.array([1, 2, 3]).sum()
```

**影响**：
- 缓存可能错误地命中
- 函数行为可能不正确

**缓解措施**：
- 当前实现：检测到 PythonObject 类型变量（包括模块）时，重新执行定义
- 这会降低缓存效率，但保证了正确性

### 7. 函数使用了动态属性访问（低风险）

**问题描述**：
- 函数使用了 `getattr`、`hasattr` 等动态属性访问
- 这些属性可能在不同时间点有不同的值
- 缓存可能无法捕获这些变化

**示例**：
```python
class A:
    pass

a = A()
a.x = 10

def f():
    return getattr(a, 'x', 0)

a.x = 20

def f():  # 缓存可能命中，但函数行为会改变
    return getattr(a, 'x', 0)
```

**影响**：
- 缓存可能错误地命中
- 函数行为可能不正确

**缓解措施**：
- 如果 `a` 是 PythonObject 类型，当前实现会重新执行定义
- 但如果 `a` 的属性变化了，缓存键不会变化

### 8. 函数使用了 exec/eval（低风险）

**问题描述**：
- 函数内部使用了 `exec` 或 `eval`
- 这些代码可能访问到不在缓存键中的变量
- 缓存可能无法捕获这些变化

**示例**：
```python
x = 10

def f():
    return eval('x')

x = 20

def f():  # 缓存可能命中，但函数行为会改变
    return eval('x')
```

**影响**：
- 缓存可能错误地命中
- 函数行为可能不正确

**缓解措施**：
- 如果 `x` 在 `variable_manager_` 中，缓存键会包含它
- 但如果 `eval` 访问了其他变量，缓存可能无法捕获

## 建议的改进方案

### 方案1：禁用对包含 PythonObject 的函数的缓存（当前实现）

**优点**：
- 简单、安全
- 保证正确性

**缺点**：
- 缓存效率低（对于包含 numpy/pandas 的函数，几乎不会命中缓存）

### 方案2：在缓存键中包含模块属性的哈希值

**优点**：
- 可以捕获模块属性的变化
- 提高缓存命中率

**缺点**：
- 实现复杂
- 需要遍历模块的所有属性，性能开销大

### 方案3：使用更细粒度的缓存失效策略

**优点**：
- 可以针对不同类型的变量使用不同的策略
- 平衡性能和正确性

**缺点**：
- 实现复杂
- 需要维护更复杂的缓存逻辑

### 方案4：提供缓存控制选项

**优点**：
- 用户可以选择是否启用缓存
- 对于关键场景，可以禁用缓存

**缺点**：
- 需要用户手动控制
- 可能影响性能

## 当前实现的权衡

当前实现选择了**方案1**：对于包含 PythonObject 类型变量的函数，即使缓存键匹配，也会重新执行定义。

**权衡**：
- ✅ **正确性优先**：确保函数行为正确
- ❌ **性能折衷**：对于包含 numpy/pandas 的函数，缓存效率较低

## 最佳实践建议

1. **避免修改模块属性**：不要修改导入模块的属性（如 `np.array`）
2. **避免修改内置函数**：不要修改内置函数（如 `len`）
3. **使用变量而非模块属性**：如果可能，将模块属性赋值给变量后再使用
4. **对于关键函数，考虑禁用缓存**：如果函数行为对正确性要求很高，可以考虑禁用缓存

## 总结

当前的缓存机制在**正确性**和**性能**之间做了权衡：
- 对于简单类型变量，缓存效率高
- 对于 PythonObject 类型变量，为了保证正确性，会重新执行定义

虽然存在一些潜在的边缘情况，但在实际使用中，这些问题很少出现。如果遇到缓存相关的问题，可以考虑：
1. 检查是否有模块属性被修改
2. 检查是否有内置函数被替换
3. 考虑禁用缓存或使用更细粒度的缓存策略

