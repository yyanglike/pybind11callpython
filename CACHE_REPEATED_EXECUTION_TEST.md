# 缓存函数重复执行性能测试

## 测试目的

验证缓存的函数如果执行多次，是否比不缓存的执行时间更短。

## 测试场景

### 场景1: 重复定义相同函数

当同一个函数被定义多次时，缓存机制应该能够：
- 第一次定义：执行 `py::exec`，将结果缓存
- 后续定义：从缓存中获取，避免重复执行 `py::exec`

### 场景2: 多次调用已定义的函数

函数定义后，多次调用函数本身不应该受缓存影响（因为函数调用不涉及缓存）。

## 测试用例

### test_cache_repeated_def.pys

这个测试用例专门测试重复定义函数的场景：

```python
# 定义同一个函数多次
def compute(x):
    return x * x + 1

def compute(x):  # 第二次定义（应该命中缓存）
    return x * x + 1

def compute(x):  # 第三次定义（应该命中缓存）
    return x * x + 1
# ... 更多重复定义

# 调用函数
result = compute(10)
```

## 运行测试

### 方法1: 使用性能统计

```bash
# 开启缓存
./build/run_pys_script --cache --stats test_cache_repeated_def.pys

# 关闭缓存
./build/run_pys_script --no-cache --stats test_cache_repeated_def.pys
```

查看 `Exec cache hits` 和 `Exec cache misses` 来确认缓存是否生效。

### 方法2: 使用性能对比脚本

```bash
./compare_repeated_def_performance.sh
```

这个脚本会：
- 运行10次测试（开启缓存）
- 运行10次测试（关闭缓存）
- 计算平均时间和加速比

## 预期结果

### 理想情况

- **开启缓存**：第一次定义执行 `py::exec`，后续定义从缓存获取
- **关闭缓存**：每次定义都执行 `py::exec`
- **加速比**：应该 > 1.0（开启缓存更快）

### 实际情况

由于延迟缓存策略（只在函数定义次数 > 1 时才启用缓存），以及缓存本身的开销（哈希计算、查找），实际结果可能：

1. **如果函数定义次数 <= 1**：缓存不会启用，开启/关闭缓存性能相近
2. **如果函数定义次数 > 1**：缓存启用，但需要权衡：
   - 缓存查找开销
   - 哈希计算开销
   - 避免 `py::exec` 的收益

## 测试结果分析

### 缓存命中率

查看性能统计中的 `Exec cache hit rate`：
- 如果 > 50%：缓存机制有效
- 如果 < 50%：可能需要调整缓存策略

### 性能提升

- **加速比 > 1.0**：缓存提升了性能 ✅
- **加速比 < 1.0**：缓存开销超过了收益 ⚠️

## 注意事项

1. **延迟缓存策略**：函数只定义一次时，缓存不会启用（避免不必要的开销）
2. **缓存开销**：哈希计算、变量状态检查、缓存查找都有开销
3. **测试环境**：不同环境下结果可能不同（CPU、内存、Python版本等）

## 优化建议

如果测试结果显示缓存未提升性能：

1. **检查缓存命中率**：使用 `--stats` 查看缓存统计
2. **调整缓存策略**：考虑修改延迟缓存阈值
3. **优化缓存键计算**：减少哈希计算开销
4. **分析具体场景**：确定是否真的需要缓存（函数是否真的被重复定义）

